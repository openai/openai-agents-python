# Airtable configuration for the PO Assistant example.
# Copy this file to `.env` and fill in values. The `.env` file is ignored by git.

# Personal Access Token scoped to the single base used by this project.
# Required scopes: data.records:read. Optional: schema.bases:read (for schema fetch).
AIRTABLE_PAT=changeme

# Airtable Base ID for the Client Portal base.
AIRTABLE_BASE_ID=appIQpYvYVDlVtAPS

