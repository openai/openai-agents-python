# This file can be empty. It helps Python recognize this directory as a package,
# though for Flask template loading, it's not strictly necessary when using
# the `template_folder` argument in the Flask app constructor.
